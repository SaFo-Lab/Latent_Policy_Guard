# Latent Policy Guard — core dependencies.
#
# These are minimum tested versions, not strict upper bounds. Newer
# point releases of transformers / peft / datasets generally work.

torch>=2.3
transformers>=4.45
peft>=0.11
datasets>=3.0
accelerate>=0.30
deepspeed>=0.14
safetensors>=0.4
sentencepiece>=0.2
pyarrow>=15
tqdm>=4.66

# Optional — only needed by evaluation/models/*_vllm plugins.
openai>=1.40
